<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GrKeranjang"></activity>
        <activity android:name=".GrDaftarPelayanan" />
        <activity
            android:name=".ui.login.GrDaftarPelayanan"
            android:label="@string/title_activity_gr_daftar_pelayanan" />
        <activity android:name=".GrJenisHewan" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MasukkanKeranjang" />
        <activity android:name=".PilihBarang" />
        <activity android:name=".PilihJenisMakanan" />
        <activity android:name=".PilihJenisHewan" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>